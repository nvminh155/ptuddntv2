import { Stack } from "expo-router";
import React from "react";

const BookLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
};

export default BookLayout;
